/**
 * Wallet feature — saldo, ledger e (futuro) saque PIX.
 *
 * STATUS: placeholder na página `/wallet` (API read-only em `/api/wallet`)
 *
 * Quando implementar, criar aqui:
 * - `api.ts` — GET saldo / extrato
 * - `components/wallet-summary.tsx`, `ledger-list.tsx`
 *
 * Chave PIX do usuário: `PATCH /api/auth/me` (campo `pixKey`) — UI em account.
 * Payouts: model Prisma existe; routes ainda não.
 */

export {};
