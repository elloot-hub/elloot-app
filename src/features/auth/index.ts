/**
 * Auth feature — login, registro, OAuth, sessão client-side.
 *
 * STATUS: ativo
 *
 * Onde mexer:
 * - `api.ts` — login/register/me/oauth
 * - `context.tsx` + `storage.ts` — token + user
 * - `components/` — forms, RequireAuth, RequireGuest
 *
 * Páginas: `/login`, `/register`, `/auth/callback`.
 */

export {};
