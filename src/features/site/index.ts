/**
 * Site feature — páginas institucionais (termos, FAQ, etc.).
 *
 * STATUS: ativo
 *
 * Conteúdo em `content/pages.ts`; render em `components/content-page.tsx`.
 * Rota dinâmica: `app/(main)/[slug]/page.tsx`.
 */

export {};
